<?xml version="1.0" encoding="utf-8"?>
<!--
    Permission gate screen shown on first launch (and any time permissions are missing).
    Displays the status of all three required permissions and a single button
    that guides the user through granting each one in order.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginBottom="24dp"
        android:src="@android:drawable/ic_lock_lock"
        app:tint="@color/colorPrimary"
        android:contentDescription="@null" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/app_name"
        android:textColor="@color/colorPrimary"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorSurface"
        android:elevation="2dp"
        android:lineSpacingMultiplier="1.4"
        android:padding="16dp"
        android:textColor="@color/colorTextPrimary"
        android:textSize="14sp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="32dp"
        tools:text="❌ App Usage Access\n❌ Device Administrator\n❌ Accessibility Service" />

    <Button
        android:id="@+id/btnGrant"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/grant_permissions"
        android:textAllCaps="false"
        android:textSize="16sp" />

</LinearLayout>
